{
  "name": "redact-pii",
  "version": "2.0.6",
  "description": "Remove personally identifiable information from text.",
  "main": "./lib/index.js",
  "scripts": {
    "test": "mocha --grep '\\[integration\\]' --invert -s 20 --timeout 10000 --reporter spec --require test/bootstrap test/*.test.js test/**/*.test.js && prettier './+(bin|lib|test)/**/*.js' --list-different",
    "prettier": "prettier './+(bin|lib|test)/**/*.+(js|ts|tsx)' --write",
    "test:coverage": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- --require test/bootstrap test/*.test.js test/**/*.test.js",
    "test:watch": "mocha --watch --reporter dot --require test/bootstrap test/*.test.js test/**/*.test.js"
  },
  "engines": {
    "node": ">6.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/solvvy/redact-pii.git"
  },
  "author": "Patrick Hulce <patrick.hulce@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://gitlab.com/solvvy/redact-pii/issues"
  },
  "homepage": "https://gitlab.com/solvvy/redact-pii#readme",
  "dependencies": {
    "@google-cloud/dlp": "^0.10.0",
    "lodash": "^4.16.6",
    "semver": "^5.5.1"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "istanbul": "^0.4.5",
    "json-loader": "^0.5.7",
    "mocha": "^5.2.0",
    "prettier": "1.14.3",
    "rewire": "^3.0.2",
    "sinon": "^1.17.6",
    "sinon-chai": "^2.8.0"
  },
  "prettier": {
    "singleQuote": true,
    "printWidth": 120
  }
}
